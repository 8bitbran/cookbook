<h1><%= @category.name.singularize %> Recipes</h1>
<ul>
    <% @category.recipes.each do |recipe| %>
        <li>
            <h2><%= recipe.title %></h2>
            <p><%= recipe.description %></p>
            <%= link_to "View Recipe", recipe_path(recipe) %>
        </li>
    <% end %>
</ul>